services:
  - type: web
    name: expense-tracker
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python expense_tracker_app.py"
    envVars:
      - key: FLASK_ENV
        value: production